

export default function transactionFormValidation(fields) {
    let isValid = true;
    let errors = {
        dueDateError: "",
        paymentDateError: "",
        checkNumberError: "",
        totalError: "",
    };
    if (fields.type === ) 
}